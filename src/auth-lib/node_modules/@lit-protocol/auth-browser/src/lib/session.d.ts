/**
 *
 * Get Session Key URI eg. lit:session:
 *
 * @returns { string }
 *
 */
export declare const getSessionKeyUri: ({ publicKey, }: {
    publicKey: string;
}) => string;
/**
 *
 * Parse resource
 *
 * @property { any } resource
 *
 * @returns { { protocol: string, resourceId: string } }
 *
 */
export declare const parseResource: ({ resource, }: {
    resource: any;
}) => {
    protocol: any;
    resourceId: any;
};
